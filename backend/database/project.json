{
  "name": "database",
  "projectType": "library",
  "root": "database",
  "sourceRoot": "database/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["dist/database"],
      "options": {
        "command": "tsc -p database/tsconfig.app.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No lint configured for database\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No tests configured for database\""
      }
    }
  }
}
